/** @gpt */
function Greet(name) {
    console.log(`Hello ${name}`);
}

/** @gpt */
function sum(arr) {
    let sum = 0;
    
    for (let i = 0; i < arr.length; i++)
        sum += arr[i];
    
    return sum;
}

